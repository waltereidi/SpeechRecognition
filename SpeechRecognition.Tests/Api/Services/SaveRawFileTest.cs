using System;
using System.Collections.Generic;
using System.Text;

namespace SpeechRecognition.Tests.Api.Services
{
    public class SaveRawFileTest
    {

        public SaveRawFileTest()
        {
            // Initialize the SaveRawFile service with necessary dependencies
            // For example, you can use a mock AppDbContext and IConfiguration
            //var mockContext = new Mock<AppDbContext>();
            //var mockConfig = new Mock<IConfiguration>();
            //var mockFile = new Mock<IFormFile>();
            //_service = new SaveRawFile(mockContext.Object, mockConfig.Object, mockFile.Object);
        }

    }
}
